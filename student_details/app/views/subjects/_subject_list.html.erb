 <table class="tb">
            <tr>
              <th>
                User_id
              </th>
              <th>
                Subject
              </th>
              <!-- <th colspan="2">
                Action
              </th> -->
            </tr>
            <% @subjects.each do |u| %>
              <tr>
                   <td>
                      <%= u.user_id %>
                   </td>  
                   <td>
                     <%= u.subject_name %>
                   </td>
                  <!-- <td><%= link_to 'Edit', "/edit/#{u.id}", data: { remote: true, toggle: "modal", target: "#myModal1" } %></td>

                   <td><%= link_to 'Delete', "/delete/#{u.id}"  ,method: :delete ,data: { confirm: 'Are you sure?',remote: true}  %>
                   </td> -->
              </tr>
            <% end %>
</table>
